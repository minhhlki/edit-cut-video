# Video Cutter Tool - Dependencies

# === Python Dependencies ===
# Không cần thư viện Python bổ sung cho chức năng cơ bản (chỉ dùng standard library)
# - tkinter: Có sẵn trong Python (cho GUI)
# - subprocess, os, sys, threading: Standard library

# === Optional: Build Windows Executable ===
# Chỉ cần nếu muốn tạo file .exe cho Windows
pyinstaller>=5.0

# === Optional: YouTube Download Feature ===
# Cần nếu muốn tải video từ YouTube
yt-dlp>=2023.12.30

# === Optional: Google Drive Upload ===
# Cần nếu muốn tự động upload lên Google Drive
google-api-python-client>=2.0.0
google-auth-httplib2>=0.1.0
google-auth-oauthlib>=0.5.0

# === System Dependencies ===
# Bạn cần cài đặt ffmpeg trên hệ thống

# Cài đặt ffmpeg:
# - Ubuntu/Debian: sudo apt-get install ffmpeg
# - macOS: brew install ffmpeg
# - Windows: Tải từ https://ffmpeg.org/download.html hoặc https://www.gyan.dev/ffmpeg/builds/
